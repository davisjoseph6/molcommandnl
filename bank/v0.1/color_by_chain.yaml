schema: molcommand-sample-bank
schema_version: 1
verb: color_by_chain
items:
  - id: color-by-chain-cartoon
    utterance: "Color the cartoon by chain"
    dsl: 'color_by_chain(sel="all", target="cartoon")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-line
    utterance: "Color the lines by chain"
    dsl: 'color_by_chain(sel="all", target="line")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-surface
    utterance: "Color the surface by chain"
    dsl: 'color_by_chain(sel="all", target="surface")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-tube
    utterance: "Color the tube by chain"
    dsl: 'color_by_chain(sel="all", target="tube")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-atom
    utterance: "Color atoms by chain"
    dsl: 'color_by_chain(sel="all", target="atom")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-cartoon-3eam
    utterance: "Color the cartoon by chain for 3eam"
    dsl: 'color_by_chain(sel="all_3eam", target="cartoon")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

  - id: color-by-chain-cartoon-1crn
    utterance: "Color the cartoon by chain for 1crn"
    dsl: 'color_by_chain(sel="all_1crn", target="cartoon")'
    entities: ["representation"]
    context: null
    tags: ["color", "chain"]

